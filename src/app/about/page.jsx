import { CONFIG } from 'src/config-global';

import { AboutView } from 'src/sections/view/about';

// ----------------------------------------------------------------------

export const metadata = { title: `Order completed` };

export default function Page() {
  return <AboutView />;
}
