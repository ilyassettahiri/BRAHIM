import { CONFIG } from 'src/config-global';

import { TermsView } from 'src/sections/view/terms';

// ----------------------------------------------------------------------

export const metadata = { title: `Order completed` };

export default function Page() {
  return <TermsView />;
}
